<nav id="header" class="navbar navbar-expand-md bg-white border-bottom sticky-top py-0" style="height: 64px;">
    <div class="container-xl px-4 h-100">

        <a class="navbar-brand fw-bold text-coral d-flex align-items-center gap-2 py-0" href="{{ path('app_home') }}">
            <i class="fa-solid fa-home"></i>
            <span>SymBnb</span>
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav" aria-controls="mainNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mainNav">
            <ul class="navbar-nav mx-auto gap-1">
                <li class="nav-item">
                    <a class="nav-link text-secondary" href="{{ path('ads_index') }}">Explore</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-secondary" href="#">Saved</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-secondary" href="#">Trips</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-secondary" href="{{ path('ads_create') }}">Host your home</a>
                </li>
            </ul>

            <div class="d-flex align-items-center gap-2">
                {% if app.user %}
                    <a href="{{ path('host_request') }}" class="d-none d-md-block nav-link text-secondary">Become a Host</a>
                {% else %}
                    <a href="{{ path('app_register') }}" class="d-none d-md-block nav-link text-secondary">Become a Host</a>
                {% endif %}

                <button class="btn btn-light btn-sm rounded-circle p-2" aria-label="Language">
                    <i class="fa-solid fa-globe text-secondary"></i>
                </button>

                {% if app.user %}
                    <div class="dropdown dropdown-hover">
                        <button class="btn btn-outline-secondary rounded-pill px-3 py-2 d-flex align-items-center gap-2" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="fa-solid fa-bars"></i>
                            <i class="fa-solid fa-user-circle fs-5"></i>
                        </button>
                        <ul class="dropdown-menu dropdown-menu-end shadow-sm border">
                            <li><a class="dropdown-item" href="{{ path('account_index') }}">My Account</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_user_bookings') }}">My Bookings</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_user_settings') }}">Edit Profile</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="{{ path('app_logout') }}">Logout</a></li>
                        </ul>
                    </div>
                {% else %}
                    <div class="d-flex align-items-center gap-2 border rounded-pill px-3 py-2">
                        <a href="{{ path('app_login') }}" class="text-secondary text-decoration-none small">Sign in</a>
                        <i class="fa-solid fa-user-circle text-secondary fs-5"></i>
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
</nav>